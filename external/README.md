# External Libraries

Place local third-party libraries, SDKs, or source checkouts here when they are
needed for experiments but should stay separate from this package's source code.

The contents of this directory are ignored by git by default. Keep installable
Python dependencies in `pyproject.toml` or `requirements.txt` when possible, and
use this directory for libraries that need to be referenced from a local path.
